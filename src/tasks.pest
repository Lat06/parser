status = { "x" | " " }

description = @{ (!NEWLINE ~ ANY)* }

task_line = {
    "-" ~ " " ~ "[" ~ status ~ "]" ~ " " ~ description ~ (NEWLINE | EOI)
}

file = {
    SOI ~ (task_line | NEWLINE)* ~ EOI
}
